# yuno_agent22

Secondary Yuneta agent — a backdoor management channel controlled by `controlcenter`. Uses a PTY for advanced remote administration features (interactive shells, remote commands) independent of the primary `yuno_agent`.

Enable this only on hosts that should be reachable from a control center.
