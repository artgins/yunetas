{
    "name": "gui-agent",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "description": "Yuneta Agent Console — web SPA (gobj-ui v2 declarative shell) to operate yuneta agents: control-plane CLI, treedb, stats. Connection and auth URLs are configured by the user and stored locally in the browser (no private data committed).",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "validate-locales": "node scripts/validate-locales.mjs",
        "prebuild": "npm run validate-locales"
    },
    "dependencies": {
        "@yuneta/gobj-js": "file:../../../kernel/js/gobj-js",
        "@yuneta/gobj-ui": "file:../../../kernel/js/gobj-ui",
        "bulma": "^1.0.4",
        "i18next": "^26.0.7"
    },
    "devDependencies": {
        "vite": "^8.0.10"
    }
}
