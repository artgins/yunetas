# esp_gobj

ESP-IDF component that exposes Yuneta's core GObj framework (`kernel/c/gobj-c/`) as an ESP-IDF library, so it can be linked into ESP32 firmware builds.

Contains the `CMakeLists.txt` glue used by the ESP-IDF component manager — no standalone build.
