# modules/c/modbus

Modbus protocol GClasses (client and/or server). Provides the transport-agnostic framing used to integrate industrial / IoT devices that speak Modbus RTU or TCP.

Check `modules/c/Kconfig` for the corresponding build option and enable it in `menuconfig` if your yuno needs Modbus.
