# esp_c_prot

ESP-IDF component that exports Yuneta's protocol GClasses (from `kernel/c/root-linux/src/c_prot_*`) as an ESP-IDF library, so they can be linked into ESP32 firmware builds.

Contains the `CMakeLists.txt` glue used by the ESP-IDF component manager — no standalone build.
