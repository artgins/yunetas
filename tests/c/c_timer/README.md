# c_timer test

Tests for the `C_TIMER` GClass — periodic scheduling, one-shot timeouts and event delivery through the `yev_loop`.

## Run

```bash
ctest -R test_c_timer --output-on-failure --test-dir build
```
