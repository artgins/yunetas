# `yev_set_gbuffer()`

**Description:**
Associates a `gbuffer` with an event.

**Parameters:**
- `void *event` - The event object.
- `void *gbuffer` - The `gbuffer` to associate.

**Return Value:**
- None.
