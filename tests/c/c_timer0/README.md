# c_timer0 test

Tests for `C_TIMER0`, the minimal/lower-overhead timer GClass. Validates basic timer operations and event generation with the smallest possible setup.

## Run

```bash
ctest -R test_c_timer0 --output-on-failure --test-dir build
```
