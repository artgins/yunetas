# ytestconfig

Validate a JSON configuration file. Parses the file and reports any syntax / structural errors — a quick sanity check before starting a yuno.

## Usage

```bash
ytestconfig <config.json>
```

Exits non-zero on parse failure.
