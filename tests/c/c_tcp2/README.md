# c_tcp2 test

Second-generation tests for `C_TCP`, using the newer child-tree / state-management API. Complements `tests/c/c_tcp`.

## Run

```bash
ctest -R test_c_tcp2 --output-on-failure --test-dir build
```
