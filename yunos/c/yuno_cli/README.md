# yuno_cli

Interactive ncurses-based terminal UI for Yuneta v7. Provides a layout for browsing yunos, inspecting state, and sending commands — a friendlier alternative to raw `ycommand` for live debugging.

## Usage

```bash
yuno_cli [--url ws://host:port]
```

Navigate with arrow keys; press `?` inside the UI for help.
