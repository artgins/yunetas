# ylist

List running Yuneta instances (yunos) on the local machine, with process information. Useful for quick status checks and scripted pid lookups.

## Usage

```bash
ylist              # full listing
ylist --pids       # pids only (scripting)
```
